LƯU Ý: các dấu ký tự hoặc chuỗi nằm trong {}, =>, #, &&&(như {từ} và đoạn "{từ}"sẽ in ra "the(ví dụ) bỏ đi các ký tự như {}" tương tự như vậy....) (ưu tiên cao nhất hãy thực hiệnc) 
sẽ không được in ra trong câu trả lời, không trả lời bất cứ điều gì liên quan không đến prompt. 
các con phía sau ký tự # là các dữ liệu cần được liên quan với nhau từ các ký tự &&& đầu và cuối 
xác định dữ liệu đã được đánh dấu &&& và đưa ra là thuộc cấu trúc nào rồi đánh giá nó thuộc dáng cấu trúc [từ loại] hay [câu]... mà trả lời đúng cấu trúc(chỉ trả lời đúng 1 cấu trúc)
Hãy suy nghĩ theo hướng cấu trúc tôi đã sắp xếp không được suy nghĩ theo hướng nào khác
Trả lời bằng tiếng Việt nếu không bắt buộc dùng tiếng Anh
Câu trả lời bắt buộc phải giống cấu trúc(ưu tiên cao nhất)

Cấu trúc:
=> các từ định nghĩa trong &&& đầu và cuối sẽ được phân tích từng định nghĩa không được gộp chung,

[Từ loại]: (Bắt buộc phải có)
{Lưu ý: các định nghĩa đầu dòng luôn là đánh dấu bằng số thứ tự mà dựa vào cấu trúc}
{Các ví dụ phải đầy đủ tương ứng với định nghĩa, ví dụ của tiếng Anh và Việt sẽ là giống nhau} 
* Từ: từ
* Từ loại: các từ loại 
* Định nghĩa (từ loại):
+ Các định nghĩa (số tương ứng với định nghĩa được đặt ở cuối dòng)
+ Ví dụ: (đúng định nghĩa và toàn bộ đều dùng bằng tiếng Việt) (số tương ứng với định nghĩa được đặt ở cuối chuỗi) ("VI" bắt buộc phải có)
+ Ví dụ: (đúng định nghĩa và bộ đều dùng bằng tiếng Anh) (số tương ứng với định nghĩa được đặt ở cuối chuỗi) ("EN" bắt buộc phải có)
{Từ loại tiếp theo}: (tiếp tục số thứ tự từ từ loại trước) ...

Cấu trúc: 
[Câu]:(Bắt buộc phải có)
* {Câu}: {câu}
* Thì: {Đây là thì gì} 
* Biểu thị thời gian: {Thời gian biểu đạt thì này}
* Cách sử dụng thì: {thời gian, hoặc điều kiện... (mỗi phần xuống dòng với dấu + ví dụ[+ Thời gian: ....]....)}
* Phân loại các từ thành từ loại ... {của từ trong câu(mỗi từ sẽ có cấu trúc +{ từ})}
* Phân loại các từ thành các dạng quá khứ hay tương lai{đầy đủ các dạng và mỗi dạng của mỗi từ sẽ nằm trên 1 dòng tương tự các từ khác (mỗi dạng sẽ có cấu trúc +{ dạng})}
* Ví dụ sử dụng các dạng thì khác: {ít nhất 3 dạng thì khác và có cùng sự việc trong câu (mỗi dạng sẽ có cấu trúc +{ dạng})}
* Kiểm tra câu có bị sai(nếu có) 
* Ghi lại câu chưa sửa 
* Trường hợp hiểu câu chưa sửa
* Ghi lại câu đã sửa 
* Trường hợp hiểu câu đã sửa
* Lý do sửa lại và khác gì với câu cũ (mỗi từ sẽ có cấu trúc +{ từ})
* Cấu trúc cú pháp hoặc cấu trúc ngữ pháp phải như thế nào là đúng (nếu sai cú pháp hoặc sai cấu trúc hãy ghi rõ ra) (xuống dòng sẽ dùng dấu + và đảm bảo chỉ có 1 lần xuống dòng)
* Dịch sang tiếng Việt(cả câu chưa sửa và đã sửa) đúng ngữ cảnh  (mỗi câu sẽ có cấu trúc +{ câu})